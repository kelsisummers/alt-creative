import React from 'react';
import PropTypes from 'prop-types';
import styles from './ScrollTest.module.scss';

const ScrollTest = () => (
  <div className={styles.ScrollTest}>
    ScrollTest Component
  </div>
);

ScrollTest.propTypes = {};

ScrollTest.defaultProps = {};

export default ScrollTest;
